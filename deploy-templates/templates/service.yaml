apiVersion: v1
kind: Service
metadata:
  name: {{ include "dnet-6-0-multi-select-app.fullname" . }}
  labels:
    {{- include "dnet-6-0-multi-select-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dnet-6-0-multi-select-app.selectorLabels" . | nindent 4 }}
