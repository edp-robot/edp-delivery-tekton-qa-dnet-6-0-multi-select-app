apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dnet-6-0-multi-select-app.fullname" . }}-test-connection"
  labels:
    {{- include "dnet-6-0-multi-select-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dnet-6-0-multi-select-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
